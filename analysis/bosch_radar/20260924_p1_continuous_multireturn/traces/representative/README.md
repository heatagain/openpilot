# Representative traces

Representative transitions and coordinate jumps are stored in `tables/representative_jumps.csv`.
The ignored full transition table is reproducible with `scripts/analyze_representative_churn.py`.
