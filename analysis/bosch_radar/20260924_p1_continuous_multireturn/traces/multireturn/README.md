# Multi-return traces

Corpus-wide compact episode evidence is stored in `tables/multi_return_clusters.csv` and
`tables/large_multireturn_summary.csv`. Large per-scan copies are intentionally not duplicated here.
